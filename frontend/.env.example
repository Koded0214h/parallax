# Parallax Backend API Gateway URL
# For local dev with Vite proxy, leave blank (requests will proxy to http://localhost:8080)
# For remote or production deployments:
VITE_API_URL=https://parallax-n4it.onrender.com
